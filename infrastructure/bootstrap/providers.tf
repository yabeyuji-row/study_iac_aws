# 手動編集: 可 - プロバイダー設定を意図して変更する。
# 概要:
# Terraform が AWS API を呼び出すためのプロバイダー設定を定義する。
#
# 詳細:
# 認証情報は環境変数、AWS CLI プロファイル、OIDC などの標準的な AWS SDK 解決順に任せる。
# このファイルにはアクセスキーやシークレットキーを絶対に書かない。

# AWS プロバイダーの基本設定。
provider "aws" {
  # ブートストラップリソースを作成するリージョンを変数から受け取る。
  region = var.aws_region
}
